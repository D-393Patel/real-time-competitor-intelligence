[project]
name = "realtime-competitor-strategy-tracker"
version = "0.1.0"
description = "Semantic competitor price intelligence for online book marketplaces."
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
